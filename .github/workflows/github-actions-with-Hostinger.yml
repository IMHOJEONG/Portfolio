name: GitHub Actions Demo
on: 
  push:
    branches:
      - dev
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 Running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 This steps is ${{ github.ref }} and started by ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🖥️ FrontEnd code install"
      - name: FrontEnd code install
        uses: npm install
      - run: echo "🖥️ FrontEnd code start"
      - name: FrontEnd code build
        uses: npm run build
      - run: echo "🍏 Build Status is ${{ job.status }}."
      - name: FrontEnd code build
        uses: npm run build
      - run: echo "🍏 Build Status is ${{ job.status }}."